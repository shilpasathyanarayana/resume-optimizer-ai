fastapi==0.115.0            # Web framework to build APIs quickly and efficiently run http://localhost:8000/docs
uvicorn[standard]==0.30.6   # ASGI server to run FastAPI applications
sqlalchemy==2.0.35          # ORM for interacting with the database in Pythonic way

# aiomysql==0.2.0             # Async MySQL driver for SQLAlchemy async operations
# pymysql==1.1.1              # Pure Python MySQL client for database connections

asyncpg==0.29.0             # Async PostgreSQL driver for SQLAlchemy async operations
psycopg2-binary==2.9.9      # Sync PostgreSQL driver (for Alembic migrations)
alembic==1.13.3             # Database migrations tool for SQLAlchemy

# redis==5.1.1                # In-memory database used for caching and Celery message broker
# celery==5.4.0               # Distributed task queue to run background jobs (e.g., resume AI processing)

python-dotenv==1.0.1        # Load environment variables from a .env file
python-multipart==0.0.12    # Parse multipart/form-data, needed for file uploads
pydantic==2.9.2             # Data validation and parsing library used for request/response models
pydantic-settings==2.5.2    # Simplifies managing app settings from environment variables
pydantic[email]             # Adds email type validation for Pydantic models
PyPDF2==3.0.1               # Read, manipulate, and extract text from PDF files
python-docx==1.1.2          # Read and manipulate DOCX (Word) documents
httpx==0.27.2               # Async HTTP client to make API calls (e.g., AI services)
bcrypt==4.2.0               # Password hashing for secure authentication
passlib[bcrypt]==1.7.4      # High-level password hashing library using bcrypt
python-jose[cryptography]==3.3.0 # JWT creation and verification for authentication
email-validator==2.1.0      # Validates email addresses during user registration

# AI api to optimise the resume with job description
# anthropic==0.34.2         # Optional: Client library for Anthropic Claude API (AI model)
# google-generativeai        # Optional: Google Gemini AI API client
groq                        # Lightweight client for AI model interactions or vector search

# payment gateway library [ can use any other gateway]
stripe==9.8.0               # Stripe SDK to integrate payment gateway (sandbox & production)